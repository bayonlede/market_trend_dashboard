# Drop your trained model files here — they are excluded from git via .gitignore.
# Upload to Railway using: railway volume cp ./models/<file>.pkl /app/models/<file>.pkl
#
# Required files:
#   best_model.pkl
#   scaler.pkl
#   label_encoder.pkl
